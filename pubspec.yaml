name: demo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase dependencies
  firebase_core: ^3.5.0
  cloud_firestore: ^5.1.0
  firebase_auth: ^5.1.0  # only if you use authentication
  image_picker: ^0.8.7+5
  firebase_storage: ^12.4.10
  path_provider: ^2.0.15
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  video_player: ^2.8.6
  youtube_player_flutter: ^8.1.0
  url_launcher: ^6.2.5



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
